# 客服 + 企业 RAG 知识库 改造计划（16 项 · 四批）

> 生成日期：2026-08-19
> 目标：现有「企业知识库管理助手」做加法，变成 **AI 客服通过调用企业知识库内容回答** 的系统。
> 核心原则：**知识库是唯一事实来源**，客服能力围绕它长出来——护栏分类 → 知识库检索回答 → 答不了就转人工/建工单，全链路引用可溯源。

---

## 第一批：RAG 底座加固（客服回答的质量前提，全部后端）

| # | 改动 | 具体内容 |
|---|---|---|
| 1 | **Rerank 重排层** | 混合检索召回 top-10+ → 重排（LLM 打分或本地 bge-reranker）→ top-5 进生成。客服场景对"答准"要求比内部知识库更高，这是基础 |
| 2 | **可回答性门槛 + 降级策略** | 证据不足时不再硬答，走降级链：换问法重检索一次 → 仍不足 → 如实告知 + 触发转人工/建工单。AI 客服防胡说的命门 |
| 3 | **检索日志留痕** | 每次检索落库：改写结果、命中明细+得分、rerank 前后对比、耗时；管理接口可查。客服答错时能回放定位 |

## 第二批：客服问答管线（核心加法，改造 qa_graph → 客服图）

| # | 改动 | 具体内容 |
|---|---|---|
| 4 | **护栏前置节点** | 入口先分类：kb_question / faq / smalltalk / ticket_intent / human_request / out_of_scope。越狱/套提示词直接挡掉；闲聊走轻量人设回复不进检索；标签归一化抗模型输出抖动 |
| 5 | **FAQ 知识类型 + Excel 批量导入导出** | 新增 FAQ 表（标准问+答案+相似问数组）：改写后 query 先 FAQ 向量匹配（阈值内直答）→ 未命中再走文档 RAG。Excel 模板+行级校验，运营人员自己维护 |
| 6 | **AI 客服人设 + 对外话术** | system prompt 模板化：企业信息、服务时间、回答风格、拒答话术、转人工话术；管理面板可编辑，存库不硬编码 |
| 7 | **满意度评价闭环** | 每条 AI 回答可 👍/👎 + 可选文字反馈，落库关联到该次检索日志（第 3 项），差评自动标记会话供人工复盘 |

## 第三批：人工协作闭环（AI 答不了的兜底，轻量版工单+转人工）

| # | 改动 | 具体内容 |
|---|---|---|
| 8 | **会话/消息模型** | 新增 conversations + messages 表：会话状态机（AI 服务中 / 待接入 / 人工服务中 / 已结束）、未读数、转人工原因和时间。复用现有 thread_id 体系 |
| 9 | **转人工** | 触发：用户明确要求 / 可回答性门槛两次不过 / 连续负面反馈。进入待接入池；管理面板新增"客服工作台"视图（待接入列表、领取、回复、结束）。第一版 SSE/轮询，不上 WebSocket |
| 10 | **轻量工单系统** | tickets（编号发号器、状态流：待处理/处理中/已解决/已关闭）+ ticket_progress 时间线。转人工时一键带会话上下文建单；工单列表页可筛选 |
| 11 | **快捷回复 + 会话标签** | 坐席常用话术库（管理面板维护）；会话打标签（如"退款咨询"）便于统计 |

## 第四批：接入与体验（前后端）

| # | 改动 | 具体内容 |
|---|---|---|
| 12 | **网页客服挂件 SDK** | 打包 kb-chat.js（一行 `<script>` 嵌入任意企业网站，气泡按钮+聊天窗），走渠道标识 + 访客临时身份，不要求登录 |
| 13 | **token 级流式 + 聊天体验包** | generate 改逐 token SSE；气泡 Markdown 渲染、复制、typing 指示、常见问题 pill（取 FAQ 高频问题） |
| 14 | **多会话管理** | 前端会话列表侧栏（工作台里坐席也需要），新建/切换/删除/重命名 |
| 15 | **知识库元数据 CRUD** | 建/改/删库（名称、描述），FAQ 与文档挂在库下；前端下拉变管理弹窗 |
| 16 | **评估脚本（轻量版）** | YAML 测试集跑：护栏分类准确率、FAQ 命中率、检索命中率、忠实度评分，输出报告 |

---

## 架构取舍

- **不引入新框架/新语言**：客服图在现有 qa_graph.py 的 LangGraph 图上加节点（guardrail → faq_lookup → retrieve → gate → generate → escalate），后端仍 FastAPI + SQLite 单进程，Prometheus / 审计 / RBAC 全部复用
- **企微/多渠道本次不做**：挂件 SDK（第 12 项）做好后，接企微只是加一个 channel 适配层的事，留到下一期
- **前端**：新增"客服工作台"视图，与现有知识库/深度研究视图并列

## 实施顺序

按表内编号顺序：第一批 1-3 打底 → 第二批 4-7 出客服能力 → 第三批 8-11 人工兜底 → 第四批 12-16 体验收尾。
