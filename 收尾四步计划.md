# 客服化改造收尾计划（四步）

> 生成日期：2026-08-19
> 背景：四批改造（Rerank / 可回答性门槛 / 检索日志 / 护栏分类 / FAQ / 人设 / 满意度 / 转人工 / 工单 / 快捷回复 / 知识库元数据）后端已落地，
> 经三路代码审查后确定收尾顺序。总原则：**先修 bug -> 再补 UI 最小闭环让能力"可见" -> 最后才是颜值重构 -> 测试兜底**。
> 当前状态：后端 86 个测试全过、前端构建通过，但新功能零测试覆盖、前端零接入。

---

## 第 1 步：修复必现 bug + 流式路径补齐（约半天）

### 1.1 两个 P0（不修演示必翻车）

| 问题 | 位置 | 修法 |
|---|---|---|
| **gate 用错证据分字段**：rerank_score 从头到尾没被 gate 使用，且 BM25-only 命中的 chunk 余弦恒为 0，关键词精确命中反而被判"答不了"去转人工 | `src/services/kb/qa_graph.py:302` | gate 优先读 `rerank_score`（注意 LLM 模式 0-10、crossencoder 模式 0-10 float 的量纲，统一归一到 0-1），无 rerank 分再回退余弦；BM25-only 命中不能按 0 分处理 |
| **FAQ 导出必现 500**：`kb_export_faqs` 引用了模块级未导入的 `Response`（唯一导入在 `prometheus_metrics` 函数体内，局部作用域不可见） | `src/main.py:1496`（导入缺失在 :26） | 模块顶部补 `from fastapi.responses import Response` |

### 1.2 P1 修复清单

| 问题 | 位置 | 修法 |
|---|---|---|
| 流式路径（前端主用）不写检索日志、不建会话转人工 | `src/main.py:999-1011` | `/kb/ask/stream` 补齐与同步路径一致的 escalate 闭环与 retrieval_log 落库；`run_qa_stream` 的 final 事件补 `search_meta` |
| 可回答性门槛阈值=0（默认关闭）时空检索仍触发 escalate | `src/services/kb/qa_graph.py:304` | `threshold <= 0` 时直接 pass，不走降级链 |
| 坐席 reply/close/tag 不校验会话归属；不存在的 conv_id 静默插孤儿消息 | `src/main.py:1629-1671` | 校验回复者 = 领取者（agent_id）、会话状态合法、conv 存在性返回 404 |
| 会话已转人工时再次 escalate 把坐席"踢出"（状态回 waiting、agent_id 残留） | `src/services/kb/conversation_store.py:91-98` | `transfer_to_human` 加状态转移约束：human/closed 状态不覆盖 |
| 工单状态无白名单、倒流转放行、非法状态可入库；assign 会复活已关闭工单 | `src/main.py:1776-1815`、`ticket_store.py:94-109` | 状态白名单校验 + 合法流转表（pending->processing->resolved->closed）+ 不存在返回 404 |
| 工单发号器 COUNT 再 INSERT 两步非原子，并发建单撞 UNIQUE 约束随机 500 | `src/services/kb/ticket_store.py:59-66` | 改单条 `INSERT ... SELECT COALESCE(MAX(...))+1` 或捕获 IntegrityError 重试 |
| 审计遗漏：转人工、agent claim/reply/close、工单 status/assign、快捷回复增删都没记审计 | `src/main.py:959-971、1610-1671、1776-1815、1836-1873` | 对齐同批代码的 `audit.record` 模式补齐 |
| rewrite 节点 LLM 调用无降级，超时直接 500 | `src/services/kb/qa_graph.py:263` | try/except 回退原问题（guardrail 节点已有同款降级可抄） |

### 1.3 顺手修的 P2（不紧急，改到就修）

- gate retry 时 `rewritten` 被置空导致检索日志丢失第一次查询；`retrieval_query` 参数从未被传（`main.py:942-957`）
- FAQ 直答/闲聊路径忠实度记 0 分污染统计（`qa_graph.py:207` 附近）
- escalate 路径不清 passages，触发一次注定无效的 generate 重试（`qa_graph.py:319-323`）
- reranker 解析失败无告警日志；正则不匹配无方括号格式（`reranker.py:84-125`）
- FAQ 内存索引重建非原子、删除触发全库重 embedding（`faq_store.py:150-158`）
- conversations.thread_id 无 UNIQUE 约束，并发建出重复会话（`conversation_store.py:65-82`）
- /kb/feedback 无限流且 question 字段不截断（`main.py:1549-1567`）
- 路由层直接戳 store 私有 `_conn`（`main.py:1643-1646`，应加 `mark_read` 方法）
- kb_meta 与 Chroma 双数据源不联动：删 meta 不清文档、kb_id 重建即"复活"旧数据

**验收**：86 个旧测试全过 + 新增回归测试（见第 4 步先修 P0 部分）+ 手工验证：知识库明确有的问题正常作答、FAQ 导出能下载、流式问答触发转人工后坐席队列可见。

---

## 第 2 步：前端最小客服闭环（1-2 天）

不动 App.vue 大结构，只补三样，让后端已有能力"可见"：

### 2.1 聊天消息加满意度按钮 👍/👎
- 每条 AI 消息气泡下加评价按钮，接 `POST /kb/feedback`（question/answer/score 字段后端已备好）
- 👎 时可弹可选文字反馈框

### 2.2 转人工状态条
- `/kb/ask`（流式 final 事件）返回的 `escalate` + `conversation_id` + `status` 字段目前被前端丢弃（`KbChat.vue:211-216`）
- 接上：escalate 时在消息流插入系统状态条"已为您转接人工客服，等待接入…"，并轮询会话状态（waiting -> human 时提示"坐席已接入"）

### 2.3 坐席工作台标签页
- 新增独立组件 `AgentDesk.vue`（不塞进 KbChat.vue），顶栏加一个入口
- 接 4+1 个接口：`GET /kb/agent/queue`（待接入列表，5-10s 轮询）、`POST /kb/agent/claim/{conv_id}`、`GET /kb/agent/conversations/{conv_id}/messages`、`POST /kb/agent/reply/{conv_id}`、`POST /kb/agent/close/{conv_id}`
- 页面就两栏：左边待接入/服务中会话列表，右边对话窗 + 快捷回复按钮（接 `GET /kb/quick-replies`）+ 一键建工单（接 `POST /kb/tickets`）
- 同时在 api.ts 里补齐这些封装（当前 api.ts 自 8 月 6 日后未更新）

**验收（演示脚本）**：两个浏览器窗口--左窗口客户提问，知识库有的问题 AI 带 [1][2] 引用秒回；知识库没有的问题 AI 诚实说不知道并显示"已转人工"；右窗口坐席工作台队列出现该会话，领取、回复、关闭、建工单全流程走通。

---

## 第 3 步：Notion 风格前端重构（用已有提示词）

> 提示词已生成（见对话记录 / 可另存），核心要点固化在其中：色值（#F7F6F3/#37352F/#2383E2）、Inter 字体、侧栏页面树、hover 才显操作、三视角信息架构。

此时才执行的原因：信息架构已清晰（客户聊天 / 坐席工作台 / 知识库管理三视角），重构是**换皮而不是补功能**，风险低得多。

1. **先删深度研究**：App.vue 里约 1000 行研究视图 + 样式、顶栏双视图切换、api.ts 的 `/research*` 封装；删完 `vue-tsc` 必须过
2. **组件拆分**：App.vue 只留布局骨架，拆出 `Sidebar.vue / ChatView.vue / AgentDesk.vue / DocsView.vue / AdminView.vue`，单文件 < 600 行；CSS 变量集中管理设计 token
3. **三区布局落地**：
   - 左侧栏 240px：工作区名、知识库列表（emoji 图标）、会话列表（接 `/kb/threads`）、设置入口（token 配置改成 Notion 式设置弹窗）
   - 主区：页面头 + tab（对话 / 文档 / 工作台 / 管理）
   - 沿用全部现有功能：SSE 流式、上传分片进度、引用展开、审计面板
4. **硬约束**：不引入 UI 组件库、不改 api.ts 已有函数签名、不改后端、CORS 保持 5174

**验收**：`npm run build` 通过 + 四大流程回归（问答流式 / 文档管理 / 知识库切换 / 坐席工作台）。

---

## 第 4 步：补测试（与第 1 步穿插进行）

新功能目前零测试，这次审出的问题一半是单测能拦住的。优先级：

| 优先级 | 测试内容 | 要点 |
|---|---|---|
| 🔴 随第 1 步写 | gate 判分回归：rerank on/off 两种模式、BM25-only 命中、阈值=0 关闭态、空检索 | 防止 P0 复发 |
| 🔴 随第 1 步写 | 工单状态机：白名单、非法流转拒绝、发号器并发 | 用线程并发测重号 |
| 🟡 | 会话状态机：transfer 踢坐席场景、claim 原子性、reply 越权 | |
| 🟡 | FAQ store：增删查、相似问拆分、索引重建后可查 | |
| 🟡 | 护栏分类：六类标签 + 越狱拦截 + 分类失败降级 | FakeLLM route 模式 |
| 🟢 | 检索日志：record/recent 往返、hits JSON 解析 | |

模式沿用现有 `tests/mocks`（FakeLLM/FakeEmbedding），不联网、毫秒级。

---

## 里程碑对照

| 完成到 | 项目对外形态 |
|---|---|
| 第 1 步 | 后端"言行一致"，但演示仍是知识库助手 |
| 第 2 步 | **可以完整演示客服闭环故事**（客户/AI/转人工/工单） |
| 第 3 步 | 演示形态达到产品级颜值（Notion 风格三视角） |
| 第 4 步 | 调参有回归手段，敢放心继续迭代 |
